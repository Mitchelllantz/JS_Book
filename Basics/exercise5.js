
// First lets make the string number into a variable called five
let five = '5';
// Then we convert the string into a number
five = Number(five);
// Now when we add the numbers together they are both the same type, because we explicity changed the type
console.log(five + 10);